# 校园公会任务流系统 中期报告

## 基本信息

**项目名称：** 校园公会任务流系统

**学 院：** 计算机学院

**小组序号：** \_43_号

**成员姓名：** 田致远 / 杨霁铭 / 刘孟轩 / 吴天皓

**指导老师：** 尹兆远

**当前版本：** 中期版

**更新日期：** 2026 年 5 月_6_日

## 一、项目概述 【中期必填】

### 1. 项目背景

校园内各类社团、学生组织（"公会"）在任务管理中普遍存在以下痛点：任务分配不透明、进度追踪困难、成员反馈不及时、缺乏统一的任务流转与状态管理平台。本项目针对校园场景下的轻量化任务协作需求，开发一套
"校园公会任务流系统"，旨在解决学生组织内部任务发布、分配、执行、反馈的全流程管理问题，提升社团协作效率，降低沟通成本。

### 2. 系统目标

- 实现任务的创建、分配、状态更新与进度追踪，支持任务生命周期全流程管理。

- 为社团管理员提供成员管理、任务统计、进度监控功能，提升组织管理效率。

- 为普通成员提供任务接收、提交、反馈的统一入口，简化任务执行流程。

- 实现前端静态页面自动化构建与部署，保障系统可访问性与版本迭代效率。

### 3. 开发环境

表格

  ----------------------------------------------------------------------------------
  **类别**     **工具 / 环境说明**
  ------------ ---------------------------------------------------------------------
  后端技术栈   Java 17 / Spring Boot / Maven / MyBatis（或其他 ORM 框架）

  前端技术栈   HTML / JavaScript /esbuild（构建工具）

  数据库       MySQL（通过 sql 目录下脚本管理表结构）

  版本控制     Git + GitHub

  CI/CD 工具   GitHub
               Actions（通过.github/workflows/deploy.yml实现前端自动化构建与部署）

  开发工具     IntelliJ IDEA / VS Code
  ----------------------------------------------------------------------------------

## 二、需求分析 【中期必填】

### 1. 功能需求

根据校园公会的任务管理场景，系统核心功能分为以下模块：

1.  **用户与权限管理模块**

    - 支持用户注册、登录，区分管理员（公会负责人）与普通成员角色。

    - 管理员可创建公会、添加成员、分配角色权限。

2.  **任务管理模块**

    - 管理员可创建任务、设置任务详情（描述、截止时间、负责人）。

    - 支持任务状态流转：待分配 → 已分配 → 执行中 → 已提交 → 已审核 /
      已驳回。

    - 成员可查看已分配任务、提交进度、上传结果、反馈问题。

3.  **任务进度统计模块**

    - 管理员可查看公会内所有任务的进度分布、成员完成率。

    - 支持按任务状态、时间范围筛选统计。

4.  **前端交互模块**

    - 提供任务列表、任务详情、个人中心等页面，支持响应式访问。

    - 前端页面通过 GitHub Pages 部署，实现静态资源的自动化构建与发布。

**核心业务流程**：管理员创建任务 → 分配给指定成员 → 成员接收并执行任务 →
提交任务结果 → 管理员审核（通过 / 驳回）→ 任务状态更新并同步至双方。

### 2. 非功能需求

- **性能要求**：系统支持至少 50
  名成员同时在线访问，页面响应时间≤2s；前端静态资源构建与部署流程耗时≤5
  分钟。

- **安全要求**：用户密码加密存储，接口请求携带身份校验信息，防止未授权访问；任务数据仅对所属公会成员可见。

- **兼容性要求**：前端页面兼容主流浏览器（Chrome、Edge、Firefox），支持
  PC 端访问；部署流程稳定可靠，每次代码提交可自动触发构建与部署。

## 三、系统设计 【中期必填】

### 1. 系统架构

本系统采用**前后端分离架构**，整体分为三层：

1.  **前端层**：基于 HTML/JavaScript 实现用户交互界面，通过 esbuild
    进行打包优化，最终部署于 GitHub Pages。

2.  **后端服务层**：基于 Spring Boot 开发 RESTful
    接口，提供用户管理、任务管理、数据统计等核心业务逻辑。

3.  **数据持久层**：通过 MyBatis（或其他 ORM 框架）操作 MySQL
    数据库，存储用户信息、公会信息、任务信息等数据。

系统整体架构图（可在报告中插入手绘或工具绘制的架构图）：

Campus-Guild-TaskFlow/

├── server/ \# Spring Boot 后端

│ └── src/main/java/\...

│ ├── config/ \# 配置类

│ ├── controller/ \# REST API

│ ├── service/ \# 业务逻辑

│ ├── model/ \# 数据模型

│ └── repository/ \# 数据访问

│

├── ui/ \# 前端

│ ├── index.html \# 入口页面

│ ├── js/

│ │ ├── app.js \# 主应用代码

│ │ └── bundle.js \# esbuild 输出

│ └── css/

│ └── style.css \# 样式

│

├── data/ \# H2 数据库文件

│ └── campusguild.mv.db

│

└── README.md

2\. 模块设计

系统按功能划分为以下核心模块，模块间职责清晰、低耦合：

表格

  -----------------------------------------------------------------------------
  **模块名称**         **核心职责**
  -------------------- --------------------------------------------------------
  用户与权限管理模块   用户注册 / 登录、角色管理、公会成员管理、权限校验

  任务管理模块         任务 CRUD、任务分配、状态流转、提交与审核功能

  数据统计模块         任务进度统计、成员完成率统计、任务状态分布统计

  前端交互模块         页面渲染、用户交互、与后端接口通信、静态资源构建与部署

  数据库模块           数据持久化、数据表管理、SQL 脚本维护
  -----------------------------------------------------------------------------

### 3. 数据库设计

#### （1）E-R 图

（中期可绘制简化版 E-R
图，展示核心实体及关系，结项再完善）核心实体：用户（User）、公会（Guild）、任务（Task）

- 用户与公会：多对多关系（一个用户可加入多个公会，一个公会包含多个用户）

- 用户与任务：一对多关系（一个用户可接收多个任务，一个任务对应一个负责人）

- 公会与任务：一对多关系（一个公会可创建多个任务，一个任务属于一个公会）

E-R 图示例（可在报告中插入绘制的图片）：

#### ![IMG_256](./images/media/image1.jpeg){width="11.4375in" height="20.0in"}（2）主要数据表设计

以下为核心数据表结构（可根据实际 sql 目录下的脚本调整）：

1.  

**用户表（user）**

2.  

表格

3.  

  ---------------------------------------------------
  **字段名**    **类型**       **说明**
  ------------- -------------- ----------------------
  id            BIGINT         主键，用户 ID

  username      VARCHAR(50)    用户名

  password      VARCHAR(100)   加密后的密码

  role          VARCHAR(20)    角色（admin/member）

  create_time   DATETIME       创建时间
  ---------------------------------------------------

4.  

  ------------------------------------------------
  **字段名**    **类型**       **说明**
  ------------- -------------- -------------------
  id            BIGINT         主键，公会 ID

  name          VARCHAR(100)   公会名称

  admin_id      BIGINT         管理员 ID，关联
                               user.id

  create_time   DATETIME       创建时间
  ------------------------------------------------

5.  

  ------------------------------------------------------------------
  **字段名**    **类型**       **说明**
  ------------- -------------- -------------------------------------
  id            BIGINT         主键，任务 ID

  guild_id      BIGINT         所属公会 ID，关联 guild.id

  title         VARCHAR(100)   任务标题

  description   TEXT           任务描述

  assigner_id   BIGINT         分配人 ID，关联 user.id

  receiver_id   BIGINT         接收人 ID，关联 user.id

  status        VARCHAR(20)    任务状态（待分配 / 执行中 / 已提交 /
                               已审核）

  deadline      DATETIME       截止时间

  submit_time   DATETIME       提交时间
  ------------------------------------------------------------------

## 四、系统实现 【中期部分填写；结项补全】

### 1. 关键技术

- **后端核心技术**：基于 Spring Boot 的分层架构设计，通过 Service
  层封装业务逻辑，Controller 层提供 RESTful 接口，实现前后端解耦；使用
  MyBatis 实现数据库操作，简化 SQL 编写。

- **前端构建与部署**：使用 esbuild 对前端 JS
  代码进行打包与压缩，提升页面加载速度；通过 GitHub Actions
  配置deploy.yml工作流，实现代码提交后自动构建前端静态资源并部署至
  GitHub Pages，无需手动操作。

- **版本控制与协作**：通过 Git
  进行代码版本管理，团队成员通过分支开发、Pull Request
  进行代码合并，保障代码质量与版本可控。

### 2. 界面展示（中期可放已完成页面 / 原型图）

以下为当前已完成的核心界面（可在报告中插入本地运行截图）：

1.  

**用户登录 / 注册页面**（截图示例：展示登录表单、注册表单界面）

![](./images/media/image2.png){width="6.7625in"
height="3.5722222222222224in"}

2.  **任务展示大厅**

![](./images/media/image3.png){width="6.7652777777777775in"
height="3.5597222222222222in"}

### 3. 核心代码片段（中期可贴关键部分）

#### （1）GitHub Actions 部署配置（deploy.yml）

yaml

姓名: 部署到 GitHub Pages在:

推:

分支: \[主要的\]

工作流调度:权限:

内容: 读

页面: 写

ID令牌: 写并发:

团体: \"页面\"

正在取消: 错误的工作:

建造:

继续: ubuntu-latest

步骤:

\- 姓名: 查看

用途: actions/checkout@v4

\- 姓名: 设置节点

用途: actions/setup-node@v4

和:

节点版本: \'20\'

\- 姓名: 安装 esbuild

跑步: npm install -g esbuild

\- 姓名: 构建前端

工作目录: ./ui

跑步: \|

npx esbuild js/app.js \--bundle \--outfile=js/bundle.js \--minify

\- 姓名: 上传工件

用途: actions/upload-pages-artifact@v3

#### （2）后端任务 Controller 示例

java

运行

\@RestController@RequestMapping(\"/api/task\")public class
TaskController {

\@Autowired

private TaskService taskService;

\@GetMapping(\"/list/{guildId}\")

public List\<Task\> getTaskList(@PathVariable Long guildId) {

return taskService.getTasksByGuildId(guildId);

}

\@PostMapping(\"/create\")

public Result createTask(@RequestBody Task task) {

taskService.createTask(task);

return Result.success();

}}

## 五、系统测试 【中期先写方案；结项补全结果】

### 1. 测试方案

- **测试目标**：验证系统核心功能的正确性、稳定性，发现并修复当前阶段存在的问题。

- **测试范围**：

  1.  后端接口测试：用户登录 / 注册接口、任务 CRUD
      接口的功能正确性与参数校验。

  2.  前端页面测试：页面渲染、按钮交互、与后端接口通信的稳定性。

  3.  自动化部署流程测试：验证deploy.yml工作流是否能正常构建并部署前端页面。

- **测试方法**：单元测试（JUnit）、接口测试（Postman）、手动功能测试。

### 2. 测试用例（中期先列出计划，结项补全结果）

表格

  ---------------------------------------------------------------------------------------------------------------
  **测试用例   **测试场景**               **预期结果**                               **测试结果（中期可留空）**
  ID**                                                                               
  ------------ -------------------------- ------------------------------------------ ----------------------------
  TC001        用户登录（正确账号密码）   成功登录，返回用户信息与 token             

  TC002        管理员创建任务             任务成功保存到数据库，前端列表显示该任务   

  TC003        成员提交任务结果           任务状态更新为 "已提交"，管理员可查看结果  

  TC004        前端代码提交触发部署       GitHub Actions 工作流成功运行，页面更新    
  ---------------------------------------------------------------------------------------------------------------

### 3. 问题与改进

- 当前问题：部分前端按钮点击无响应，存在状态值不匹配的问题（已通过修改onclick为data-action属性解决）；部分后端接口参数校验不完善。

- 改进措施：优化前端交互逻辑，完善接口参数校验；补充单元测试用例，提升代码稳定性。

## 六、用户手册 【结项补全，中期可暂留空】

### 1. 安装部署说明

（结项阶段补充：环境配置、后端部署步骤、前端部署步骤）

### 2. 操作指南

（结项阶段补充：用户注册登录、管理员创建任务、成员提交任务等操作步骤）

## 七、项目总结 【中期写阶段总结；结项补全】

### 1. 阶段成果总结

- 已完成核心数据库表设计与后端基础框架搭建，实现了用户登录、任务创建 /
  查询的基础接口。

- 已完成前端页面原型开发，实现了任务列表、登录页面的基础渲染，并解决了按钮交互问题。

- 配置完成 GitHub Actions
  自动化部署流程，实现了前端代码提交后自动构建与部署。

- 完成中期阶段需求分析、系统架构设计，明确了后续开发计划。

### 2. 不足与后续改进方向

- 当前不足：部分任务流转功能（审核、驳回）尚未实现；前端页面交互细节不完善；单元测试覆盖率较低。

- 后续计划：

  1.  完成任务全流程状态流转功能，实现管理员审核与成员反馈闭环。

  2.  优化前端页面交互体验，完善错误提示与状态反馈。

  3.  补充单元测试与接口测试，提升系统稳定性。

  4.  完善数据统计模块，实现任务进度可视化展示。

### 3. 成员分工表

表格

  ----------------------------------------------------------------------------------
  **姓名**   **班级**   **学号**       **Git 账号**     **承担任务**
  ---------- ---------- -------------- ---------------- ----------------------------
  刘孟轩     计算机 1   202405801207   Liu12138-777     后端框架搭建、用户 /
             班                                         任务接口开发

  杨霁铭     计算机 1   202405720216   yjm1153          前端页面开发、GitHub Actions
             班                                         部署配置

  吴天皓     计算机 1   202405800919   wutianhao1       数据库设计、SQL 脚本编写
             班                                         

  田致远     计算机 1   202405710723   tian-123-maker   测试方案设计、中期报告撰写
             班                                         
  ----------------------------------------------------------------------------------

### Git 提交记录（中期可截图）![](./images/media/image4.png){width="6.7659722222222225in" height="4.470138888888889in"}

## 附录 【中期可留空；结项补全】

- 参考资料：Spring Boot 官方文档、GitHub Pages 部署指南、esbuild
  使用文档

- 源代码仓库链接：<https://github.com/yjm1153/>校园公会任务流

- 其他补充材料：项目原型设计稿、SQL 脚本文件
